echo Step 1: Add all files to staging
git add .

echo Step 2: Commit changes
git commit -m "Automated website update"

echo Step 3: Make sure branch is main
git branch -M main

echo Step 4: Link repo (ignore if it already exists)
git remote add origin https://github.com/acf98eph/portfolio-website.git

echo Step 5: Push changes to GitHub
git push -u origin main
